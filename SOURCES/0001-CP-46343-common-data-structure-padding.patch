From 7c024703835f417aa3b271fbdf96b2d7778abd80 Mon Sep 17 00:00:00 2001
From: Chunjie Zhu <chunjie.zhu@cloud.com>
Date: Fri, 18 Jul 2025 18:02:35 +0800
Subject: [PATCH 1/2] CP-46343: common data structure padding

Signed-off-by: Chunjie Zhu <chunjie.zhu@cloud.com>
---
 arch/x86/include/asm/processor.h    |  1 +
 block/elevator.h                    |  4 ++++
 drivers/char/ipmi/ipmi_si.h         |  2 ++
 drivers/char/ipmi/ipmi_si_sm.h      |  2 ++
 drivers/char/ipmi/kcs_bmc_client.h  |  2 ++
 drivers/pci/pci.h                   |  1 +
 drivers/scsi/sd.h                   |  2 ++
 include/linux/backing-dev-defs.h    |  2 ++
 include/linux/blk-integrity.h       |  2 ++
 include/linux/blk-mq.h              |  8 ++++++++
 include/linux/blk_types.h           |  2 ++
 include/linux/blkdev.h              | 10 ++++++++++
 include/linux/bsg-lib.h             |  2 ++
 include/linux/device.h              |  1 +
 include/linux/device/bus.h          |  2 ++
 include/linux/device/class.h        |  2 ++
 include/linux/ethtool.h             |  2 ++
 include/linux/fs.h                  |  1 +
 include/linux/fwnode.h              |  1 +
 include/linux/ipmi.h                |  4 ++++
 include/linux/ipmi_smi.h            |  3 +++
 include/linux/ipv6.h                |  2 ++
 include/linux/irq.h                 |  1 +
 include/linux/irq_work.h            |  1 +
 include/linux/irqdesc.h             |  1 +
 include/linux/irqdomain.h           |  2 ++
 include/linux/mempolicy.h           |  1 +
 include/linux/mempool.h             |  2 ++
 include/linux/memremap.h            |  3 +++
 include/linux/mm.h                  |  2 ++
 include/linux/mm_types.h            |  2 ++
 include/linux/module.h              |  1 +
 include/linux/net.h                 |  4 ++++
 include/linux/netdevice.h           | 13 +++++++++++++
 include/linux/netlink.h             |  2 ++
 include/linux/nvme-fc-driver.h      |  2 ++
 include/linux/pagemap.h             |  1 +
 include/linux/pci.h                 |  5 +++++
 include/linux/sched.h               | 10 ++++++++++
 include/linux/sched/signal.h        |  1 +
 include/linux/sched/topology.h      |  2 ++
 include/linux/sched/user.h          |  2 ++
 include/linux/shrinker.h            |  1 +
 include/linux/skbuff.h              |  2 ++
 include/linux/swap.h                |  1 +
 include/linux/timer.h               |  1 +
 include/linux/trace_events.h        |  3 +++
 include/linux/usb/hcd.h             |  2 ++
 include/net/dcbnl.h                 |  2 ++
 include/net/devlink.h               |  2 ++
 include/net/dst.h                   |  2 ++
 include/net/dst_ops.h               |  2 ++
 include/net/fib_rules.h             |  2 ++
 include/net/flow.h                  |  6 ++++++
 include/net/genetlink.h             |  8 ++++++++
 include/net/ip6_fib.h               |  3 +++
 include/net/ip_fib.h                |  6 ++++++
 include/net/l3mdev.h                |  2 ++
 include/net/lwtunnel.h              |  3 +++
 include/net/macsec.h                |  2 ++
 include/net/neighbour.h             |  3 +++
 include/net/netdev_rx_queue.h       |  2 ++
 include/net/netlink.h               |  2 ++
 include/net/nexthop.h               |  2 ++
 include/net/rtnetlink.h             |  2 ++
 include/net/sch_generic.h           |  3 +++
 include/net/sock.h                  |  4 ++++
 include/net/tls.h                   |  6 ++++++
 include/net/udp_tunnel.h            |  8 ++++++++
 include/net/xfrm.h                  |  4 ++++
 include/net/xsk_buff_pool.h         |  1 +
 include/scsi/scsi_cmnd.h            |  2 ++
 include/scsi/scsi_device.h          |  6 ++++++
 include/scsi/scsi_host.h            |  4 ++++
 include/scsi/scsi_transport.h       |  2 ++
 include/scsi/scsi_transport_fc.h    |  2 ++
 include/scsi/scsi_transport_iscsi.h |  2 ++
 include/scsi/scsi_transport_sas.h   |  2 ++
 kernel/sched/sched.h                | 14 ++++++++++++++
 net/devlink/devl_internal.h         |  1 +
 80 files changed, 238 insertions(+)

diff --git a/arch/x86/include/asm/processor.h b/arch/x86/include/asm/processor.h
index 428348e7f06c..31c8bfa6bb13 100644
--- a/arch/x86/include/asm/processor.h
+++ b/arch/x86/include/asm/processor.h
@@ -160,6 +160,7 @@ struct cpuinfo_x86 {
 	/* Address space bits used by the cache internally */
 	u8			x86_cache_bits;
 	unsigned		initialized : 1;
+	void		*xs_kabi_padding;
 } __randomize_layout;
 
 #define X86_VENDOR_INTEL	0
diff --git a/block/elevator.h b/block/elevator.h
index 7ca3d7b6ed82..e12c8e98e493 100644
--- a/block/elevator.h
+++ b/block/elevator.h
@@ -48,6 +48,8 @@ struct elevator_mq_ops {
 	struct request *(*next_request)(struct request_queue *, struct request *);
 	void (*init_icq)(struct io_cq *);
 	void (*exit_icq)(struct io_cq *);
+
+	void	*xs_kabi_padding;
 };
 
 #define ELV_NAME_MAX	(16)
@@ -84,6 +86,8 @@ struct elevator_type
 	/* managed by elevator core */
 	char icq_cache_name[ELV_NAME_MAX + 6];	/* elvname + "_io_cq" */
 	struct list_head list;
+
+	void	*xs_kabi_padding;
 };
 
 static inline bool elevator_tryget(struct elevator_type *e)
diff --git a/drivers/char/ipmi/ipmi_si.h b/drivers/char/ipmi/ipmi_si.h
index a7ead2a4c753..c8780586b004 100644
--- a/drivers/char/ipmi/ipmi_si.h
+++ b/drivers/char/ipmi/ipmi_si.h
@@ -66,6 +66,8 @@ struct si_sm_io {
 	u8 slave_addr;
 	enum si_type si_type;
 	struct device *dev;
+
+	void *xs_kabi_padding;
 };
 
 int ipmi_si_add_smi(struct si_sm_io *io);
diff --git a/drivers/char/ipmi/ipmi_si_sm.h b/drivers/char/ipmi/ipmi_si_sm.h
index c3cdbcab0f77..a6522a651806 100644
--- a/drivers/char/ipmi/ipmi_si_sm.h
+++ b/drivers/char/ipmi/ipmi_si_sm.h
@@ -94,6 +94,8 @@ struct si_sm_handlers {
 
 	/* Return the size of the SMI structure in bytes. */
 	int (*size)(void);
+
+	void *xs_kabi_padding;
 };
 
 /* Current state machines that we can use. */
diff --git a/drivers/char/ipmi/kcs_bmc_client.h b/drivers/char/ipmi/kcs_bmc_client.h
index 6fdcde0a7169..b5901be63fbf 100644
--- a/drivers/char/ipmi/kcs_bmc_client.h
+++ b/drivers/char/ipmi/kcs_bmc_client.h
@@ -11,6 +11,7 @@
 struct kcs_bmc_driver_ops {
 	int (*add_device)(struct kcs_bmc_device *kcs_bmc);
 	int (*remove_device)(struct kcs_bmc_device *kcs_bmc);
+	void *xs_kabi_padding;
 };
 
 struct kcs_bmc_driver {
@@ -21,6 +22,7 @@ struct kcs_bmc_driver {
 
 struct kcs_bmc_client_ops {
 	irqreturn_t (*event)(struct kcs_bmc_client *client);
+	void *xs_kabi_padding;
 };
 
 struct kcs_bmc_client {
diff --git a/drivers/pci/pci.h b/drivers/pci/pci.h
index a2099277e104..99f73ccdedbf 100644
--- a/drivers/pci/pci.h
+++ b/drivers/pci/pci.h
@@ -313,6 +313,7 @@ struct pci_sriov {
 	u16		subsystem_device; /* VF subsystem device */
 	resource_size_t	barsz[PCI_SRIOV_NUM_BARS];	/* VF BAR size */
 	bool		drivers_autoprobe; /* Auto probing of VFs by driver */
+	void		*xs_kabi_padding;
 };
 
 #ifdef CONFIG_PCI_DOE
diff --git a/drivers/scsi/sd.h b/drivers/scsi/sd.h
index 409dda5350d1..5f08f03677f0 100644
--- a/drivers/scsi/sd.h
+++ b/drivers/scsi/sd.h
@@ -151,6 +151,8 @@ struct scsi_disk {
 	unsigned	urswrz : 1;
 	unsigned	security : 1;
 	unsigned	ignore_medium_access_errors : 1;
+
+	void		*xs_kabi_padding;
 };
 #define to_scsi_disk(obj) container_of(obj, struct scsi_disk, disk_dev)
 
diff --git a/include/linux/backing-dev-defs.h b/include/linux/backing-dev-defs.h
index 2ad261082bba..e8e4818905b0 100644
--- a/include/linux/backing-dev-defs.h
+++ b/include/linux/backing-dev-defs.h
@@ -158,6 +158,8 @@ struct bdi_writeback {
 		struct rcu_head rcu;
 	};
 #endif
+
+	void *xs_kabi_padding;
 };
 
 struct backing_dev_info {
diff --git a/include/linux/blk-integrity.h b/include/linux/blk-integrity.h
index f7cc8080672c..b2317e120812 100644
--- a/include/linux/blk-integrity.h
+++ b/include/linux/blk-integrity.h
@@ -33,6 +33,8 @@ struct blk_integrity_profile {
 	integrity_prepare_fn		*prepare_fn;
 	integrity_complete_fn		*complete_fn;
 	const char			*name;
+
+	void				*xs_kabi_padding;
 };
 
 #ifdef CONFIG_BLK_DEV_INTEGRITY
diff --git a/include/linux/blk-mq.h b/include/linux/blk-mq.h
index 1d482c2aabbd..aa38e692b32a 100644
--- a/include/linux/blk-mq.h
+++ b/include/linux/blk-mq.h
@@ -424,6 +424,8 @@ struct blk_mq_hw_ctx {
 	struct dentry		*sched_debugfs_dir;
 #endif
 
+	void	*xs_kabi_padding;
+
 	/**
 	 * @hctx_list: if this hctx is not in use, this is an entry in
 	 * q->unused_hctx_list.
@@ -515,6 +517,8 @@ struct blk_mq_tag_set {
 	struct mutex		tag_list_lock;
 	struct list_head	tag_list;
 	struct srcu_struct	*srcu;
+
+	void	*xs_kabi_padding;
 };
 
 /**
@@ -526,6 +530,8 @@ struct blk_mq_tag_set {
 struct blk_mq_queue_data {
 	struct request *rq;
 	bool last;
+
+	void	*xs_kabi_padding;
 };
 
 typedef bool (busy_tag_iter_fn)(struct request *, void *);
@@ -645,6 +651,8 @@ struct blk_mq_ops {
 	 */
 	void (*show_rq)(struct seq_file *m, struct request *rq);
 #endif
+
+	void	*xs_kabi_padding;
 };
 
 enum {
diff --git a/include/linux/blk_types.h b/include/linux/blk_types.h
index 92c8997b1938..dd847cb9cf9d 100644
--- a/include/linux/blk_types.h
+++ b/include/linux/blk_types.h
@@ -316,6 +316,8 @@ struct bio {
 
 	struct bio_set		*bi_pool;
 
+	void				*xs_kabi_padding;
+
 	/*
 	 * We can inline a number of vecs at the end of the bio, to avoid
 	 * double allocations for a small number of bio_vecs. This member
diff --git a/include/linux/blkdev.h b/include/linux/blkdev.h
index ef35e9a9878c..8f95da0914ed 100644
--- a/include/linux/blkdev.h
+++ b/include/linux/blkdev.h
@@ -110,6 +110,8 @@ struct blk_integrity {
 	unsigned char				tuple_size;
 	unsigned char				interval_exp;
 	unsigned char				tag_size;
+
+	void	*xs_kabi_padding;
 };
 
 typedef unsigned int __bitwise blk_mode_t;
@@ -207,6 +209,8 @@ struct gendisk {
 	 * devices that do not have multiple independent access ranges.
 	 */
 	struct blk_independent_access_ranges *ia_ranges;
+
+	void	*xs_kabi_padding;
 };
 
 static inline bool disk_live(struct gendisk *disk)
@@ -337,6 +341,8 @@ struct queue_limits {
 	 * due to possible offsets.
 	 */
 	unsigned int		dma_alignment;
+
+	void	*xs_kabi_padding;
 };
 
 typedef int (*report_zones_cb)(struct blk_zone *zone, unsigned int idx,
@@ -536,6 +542,8 @@ struct request_queue {
 	struct mutex		debugfs_mutex;
 
 	bool			mq_sysfs_init_done;
+
+	void	*xs_kabi_padding;
 };
 
 /* Keep blk_queue_flag_name[] in sync with the definitions below */
@@ -1418,6 +1426,8 @@ struct block_device_operations {
 	 * driver.
 	 */
 	int (*alternative_gpt_sector)(struct gendisk *disk, sector_t *sector);
+
+	void	*xs_kabi_padding;
 };
 
 #ifdef CONFIG_COMPAT
diff --git a/include/linux/bsg-lib.h b/include/linux/bsg-lib.h
index 9e97ced2896d..96ca74240804 100644
--- a/include/linux/bsg-lib.h
+++ b/include/linux/bsg-lib.h
@@ -60,6 +60,8 @@ struct bsg_job {
 	struct bio *bidi_bio;
 
 	void *dd_data;		/* Used for driver-specific storage */
+
+	void	*xs_kabi_padding;
 };
 
 void bsg_job_done(struct bsg_job *job, int result,
diff --git a/include/linux/device.h b/include/linux/device.h
index 3627b26b243e..d2ebaa0a173c 100644
--- a/include/linux/device.h
+++ b/include/linux/device.h
@@ -805,6 +805,7 @@ struct device {
 #ifdef CONFIG_DMA_OPS_BYPASS
 	bool			dma_ops_bypass : 1;
 #endif
+	void			*xs_kabi_padding;
 };
 
 /**
diff --git a/include/linux/device/bus.h b/include/linux/device/bus.h
index ae10c4322754..7eb327daae30 100644
--- a/include/linux/device/bus.h
+++ b/include/linux/device/bus.h
@@ -107,6 +107,8 @@ struct bus_type {
 	const struct iommu_ops *iommu_ops;
 
 	bool need_parent_lock;
+
+	void	*xs_kabi_padding;
 };
 
 int __must_check bus_register(const struct bus_type *bus);
diff --git a/include/linux/device/class.h b/include/linux/device/class.h
index abf3d3bfb6fe..6f1d9afedc3c 100644
--- a/include/linux/device/class.h
+++ b/include/linux/device/class.h
@@ -69,6 +69,8 @@ struct class {
 	void (*get_ownership)(const struct device *dev, kuid_t *uid, kgid_t *gid);
 
 	const struct dev_pm_ops *pm;
+
+	void			*xs_kabi_padding;
 };
 
 struct class_dev_iter {
diff --git a/include/linux/ethtool.h b/include/linux/ethtool.h
index 1b523fd48586..6fc039315973 100644
--- a/include/linux/ethtool.h
+++ b/include/linux/ethtool.h
@@ -912,6 +912,8 @@ struct ethtool_ops {
 	int	(*set_mm)(struct net_device *dev, struct ethtool_mm_cfg *cfg,
 			  struct netlink_ext_ack *extack);
 	void	(*get_mm_stats)(struct net_device *dev, struct ethtool_mm_stats *stats);
+
+	void	*xs_kabi_padding;
 };
 
 int ethtool_check_ops(const struct ethtool_ops *ops);
diff --git a/include/linux/fs.h b/include/linux/fs.h
index b641a01512fb..dce2627aa8a8 100644
--- a/include/linux/fs.h
+++ b/include/linux/fs.h
@@ -489,6 +489,7 @@ struct address_space {
 	spinlock_t		private_lock;
 	struct list_head	private_list;
 	void			*private_data;
+	void			*xs_kabi_padding;
 } __attribute__((aligned(sizeof(long)))) __randomize_layout;
 	/*
 	 * On most architectures that alignment is already the case; but
diff --git a/include/linux/fwnode.h b/include/linux/fwnode.h
index 7efb4493e51c..85dc33914d95 100644
--- a/include/linux/fwnode.h
+++ b/include/linux/fwnode.h
@@ -45,6 +45,7 @@ struct fwnode_handle {
 	struct list_head suppliers;
 	struct list_head consumers;
 	u8 flags;
+	void *xs_kabi_padding;
 };
 
 /*
diff --git a/include/linux/ipmi.h b/include/linux/ipmi.h
index a1c9c0d48ebf..34c2bda08b9d 100644
--- a/include/linux/ipmi.h
+++ b/include/linux/ipmi.h
@@ -70,6 +70,8 @@ struct ipmi_recv_msg {
 	 * the size or existence of this, since it may change.
 	 */
 	unsigned char   msg_data[IPMI_MAX_MSG_LENGTH];
+
+	void *xs_kabi_padding;
 };
 
 #define INIT_IPMI_RECV_MSG(done_handler) \
@@ -110,6 +112,8 @@ struct ipmi_user_hndl {
 	 * if it is not usable.
 	 */
 	void (*shutdown)(void *handler_data);
+
+	void *xs_kabi_padding;
 };
 
 /* Create a new user of the IPMI layer on the given interface number. */
diff --git a/include/linux/ipmi_smi.h b/include/linux/ipmi_smi.h
index 5d69820d8b02..7d763312a2a5 100644
--- a/include/linux/ipmi_smi.h
+++ b/include/linux/ipmi_smi.h
@@ -123,6 +123,7 @@ struct ipmi_smi_msg {
 	 * (presumably to free it).
 	 */
 	void (*done)(struct ipmi_smi_msg *msg);
+	void *xs_kabi_padding;
 };
 
 #define INIT_IPMI_SMI_MSG(done_handler) \
@@ -218,6 +219,8 @@ struct ipmi_smi_handlers {
 	 * block.
 	 */
 	void (*set_maintenance_mode)(void *send_info, bool enable);
+
+	void *xs_kabi_padding;
 };
 
 struct ipmi_device_id {
diff --git a/include/linux/ipv6.h b/include/linux/ipv6.h
index af8a771a053c..53a9a33d4221 100644
--- a/include/linux/ipv6.h
+++ b/include/linux/ipv6.h
@@ -84,6 +84,8 @@ struct ipv6_devconf {
 	__u8		ndisc_evict_nocarrier;
 
 	struct ctl_table_header *sysctl_header;
+
+	void		*xs_kabi_padding;
 };
 
 struct ipv6_params {
diff --git a/include/linux/irq.h b/include/linux/irq.h
index 90081afa10ce..75909c6faf8e 100644
--- a/include/linux/irq.h
+++ b/include/linux/irq.h
@@ -187,6 +187,7 @@ struct irq_data {
 	struct irq_data		*parent_data;
 #endif
 	void			*chip_data;
+	void			*xs_kabi_padding;
 };
 
 /*
diff --git a/include/linux/irq_work.h b/include/linux/irq_work.h
index 136f2980cba3..679b9576a2ba 100644
--- a/include/linux/irq_work.h
+++ b/include/linux/irq_work.h
@@ -18,6 +18,7 @@ struct irq_work {
 	struct __call_single_node node;
 	void (*func)(struct irq_work *);
 	struct rcuwait irqwait;
+	void *xs_kabi_padding;
 };
 
 #define __IRQ_WORK_INIT(_func, _flags) (struct irq_work){	\
diff --git a/include/linux/irqdesc.h b/include/linux/irqdesc.h
index d9451d456a73..822b79e7ecaa 100644
--- a/include/linux/irqdesc.h
+++ b/include/linux/irqdesc.h
@@ -105,6 +105,7 @@ struct irq_desc {
 #ifdef CONFIG_HARDIRQS_SW_RESEND
 	struct hlist_node	resend_node;
 #endif
+	void		*xs_kabi_padding;
 } ____cacheline_internodealigned_in_smp;
 
 #ifdef CONFIG_SPARSE_IRQ
diff --git a/include/linux/irqdomain.h b/include/linux/irqdomain.h
index 51c254b7fec2..fad625faadb4 100644
--- a/include/linux/irqdomain.h
+++ b/include/linux/irqdomain.h
@@ -170,6 +170,8 @@ struct irq_domain {
 	const struct msi_parent_ops	*msi_parent_ops;
 #endif
 
+	void *xs_kabi_padding;
+
 	/* reverse map data. The linear map gets appended to the irq_domain */
 	irq_hw_number_t			hwirq_max;
 	unsigned int			revmap_size;
diff --git a/include/linux/mempolicy.h b/include/linux/mempolicy.h
index d232de7cdc56..87f91b90f44b 100644
--- a/include/linux/mempolicy.h
+++ b/include/linux/mempolicy.h
@@ -52,6 +52,7 @@ struct mempolicy {
 		nodemask_t cpuset_mems_allowed;	/* relative to these nodes */
 		nodemask_t user_nodemask;	/* nodemask passed by user */
 	} w;
+	void *xs_kabi_padding;
 };
 
 /*
diff --git a/include/linux/mempool.h b/include/linux/mempool.h
index 4aae6c06c5f2..1c67da377780 100644
--- a/include/linux/mempool.h
+++ b/include/linux/mempool.h
@@ -23,6 +23,8 @@ typedef struct mempool_s {
 	mempool_alloc_t *alloc;
 	mempool_free_t *free;
 	wait_queue_head_t wait;
+
+	void	*xs_kabi_padding;
 } mempool_t;
 
 static inline bool mempool_initialized(mempool_t *pool)
diff --git a/include/linux/memremap.h b/include/linux/memremap.h
index 1314d9c5f05b..4ccc69ce857c 100644
--- a/include/linux/memremap.h
+++ b/include/linux/memremap.h
@@ -25,6 +25,7 @@ struct vmem_altmap {
 	unsigned long free;
 	unsigned long align;
 	unsigned long alloc;
+	void *xs_kabi_padding;
 };
 
 /*
@@ -99,6 +100,7 @@ struct dev_pagemap_ops {
 	 */
 	int (*memory_failure)(struct dev_pagemap *pgmap, unsigned long pfn,
 			      unsigned long nr_pages, int mf_flags);
+	void *xs_kabi_padding;
 };
 
 #define PGMAP_ALTMAP_VALID	(1 << 0)
@@ -133,6 +135,7 @@ struct dev_pagemap {
 	const struct dev_pagemap_ops *ops;
 	void *owner;
 	int nr_range;
+	void *xs_kabi_padding;
 	union {
 		struct range range;
 		DECLARE_FLEX_ARRAY(struct range, ranges);
diff --git a/include/linux/mm.h b/include/linux/mm.h
index 209370f64436..0e397de0e552 100644
--- a/include/linux/mm.h
+++ b/include/linux/mm.h
@@ -553,6 +553,7 @@ struct vm_fault {
 					 * page table to avoid allocation from
 					 * atomic context.
 					 */
+	void *xs_kabi_padding;
 };
 
 /*
@@ -632,6 +633,7 @@ struct vm_operations_struct {
 	 */
 	struct page *(*find_special_page)(struct vm_area_struct *vma,
 					  unsigned long addr);
+	void *xs_kabi_padding;
 };
 
 #ifdef CONFIG_NUMA_BALANCING
diff --git a/include/linux/mm_types.h b/include/linux/mm_types.h
index e77d4a5c0bac..6d641c803d8e 100644
--- a/include/linux/mm_types.h
+++ b/include/linux/mm_types.h
@@ -720,6 +720,7 @@ struct vm_area_struct {
 	struct vma_numab_state *numab_state;	/* NUMA Balancing state */
 #endif
 	struct vm_userfaultfd_ctx vm_userfaultfd_ctx;
+	void	*xs_kabi_padding;
 } __randomize_layout;
 
 #ifdef CONFIG_SCHED_MM_CID
@@ -977,6 +978,7 @@ struct mm_struct {
 #endif
 		} lru_gen;
 #endif /* CONFIG_LRU_GEN */
+	void	*xs_kabi_padding;
 	} __randomize_layout;
 
 	/*
diff --git a/include/linux/module.h b/include/linux/module.h
index f58d1eb260fa..c13540d45e78 100644
--- a/include/linux/module.h
+++ b/include/linux/module.h
@@ -590,6 +590,7 @@ struct module {
 #ifdef CONFIG_DYNAMIC_DEBUG_CORE
 	struct _ddebug_info dyndbg_info;
 #endif
+	void	*xs_kabi_padding;
 } ____cacheline_aligned __randomize_layout;
 #ifndef MODULE_ARCH_INIT
 #define MODULE_ARCH_INIT {}
diff --git a/include/linux/net.h b/include/linux/net.h
index c9b4a63791a4..0dddcf02c98c 100644
--- a/include/linux/net.h
+++ b/include/linux/net.h
@@ -102,6 +102,8 @@ struct socket_wq {
 	struct fasync_struct	*fasync_list;
 	unsigned long		flags; /* %SOCKWQ_ASYNC_NOSPACE, etc */
 	struct rcu_head		rcu;
+
+	void		*xs_kabi_padding;
 } ____cacheline_aligned_in_smp;
 
 /**
@@ -223,6 +225,8 @@ struct proto_ops {
 	int		(*sendmsg_locked)(struct sock *sk, struct msghdr *msg,
 					  size_t size);
 	int		(*set_rcvlowat)(struct sock *sk, int val);
+
+	void		*xs_kabi_padding;
 };
 
 #define DECLARE_SOCKADDR(type, dst, src)	\
diff --git a/include/linux/netdevice.h b/include/linux/netdevice.h
index 030d9de2ba2d..a4f8f79c5aae 100644
--- a/include/linux/netdevice.h
+++ b/include/linux/netdevice.h
@@ -319,6 +319,8 @@ struct header_ops {
 				const unsigned char *haddr);
 	bool	(*validate)(const char *ll_header, unsigned int len);
 	__be16	(*parse_protocol)(const struct sk_buff *skb);
+
+	void	*xs_kabi_padding;
 };
 
 /* These flag bits are private to the generic network queueing
@@ -380,6 +382,8 @@ struct napi_struct {
 	/* control-path-only fields follow */
 	struct list_head	dev_list;
 	struct hlist_node	napi_hash_node;
+
+	void	*xs_kabi_padding;
 };
 
 enum {
@@ -658,6 +662,8 @@ struct netdev_queue {
 #ifdef CONFIG_BQL
 	struct dql		dql;
 #endif
+
+	void	*xs_kabi_padding;
 } ____cacheline_aligned_in_smp;
 
 extern int sysctl_fb_tunnels_only_for_init_net;
@@ -1038,6 +1044,7 @@ struct xfrmdev_ops {
 	int	(*xdo_dev_policy_add) (struct xfrm_policy *x, struct netlink_ext_ack *extack);
 	void	(*xdo_dev_policy_delete) (struct xfrm_policy *x);
 	void	(*xdo_dev_policy_free) (struct xfrm_policy *x);
+	void	*xs_kabi_padding;
 };
 #endif
 
@@ -1645,6 +1652,8 @@ struct net_device_ops {
 	int			(*ndo_hwtstamp_set)(struct net_device *dev,
 						    struct kernel_hwtstamp_config *kernel_config,
 						    struct netlink_ext_ack *extack);
+
+	void	*xs_kabi_padding;
 };
 
 /**
@@ -2417,6 +2426,8 @@ struct net_device {
 	struct rtnl_hw_stats64	*offload_xstats_l3;
 
 	struct devlink_port	*devlink_port;
+
+	void	*xs_kabi_padding;
 };
 #define to_net_dev(d) container_of(d, struct net_device, dev)
 
@@ -2717,6 +2728,8 @@ struct packet_type {
 	struct net		*af_packet_net;
 	void			*af_packet_priv;
 	struct list_head	list;
+
+	void			*xs_kabi_padding;
 };
 
 struct offload_callbacks {
diff --git a/include/linux/netlink.h b/include/linux/netlink.h
index e8d713a37d17..15ae58793fcc 100644
--- a/include/linux/netlink.h
+++ b/include/linux/netlink.h
@@ -88,6 +88,8 @@ struct netlink_ext_ack {
 	u8 cookie[NETLINK_MAX_COOKIE_LEN];
 	u8 cookie_len;
 	char _msg_buf[NETLINK_MAX_FMTMSG_LEN];
+
+	void	*xs_kabi_padding;
 };
 
 /* Always use this macro, this allows later putting the
diff --git a/include/linux/nvme-fc-driver.h b/include/linux/nvme-fc-driver.h
index 4109f1bd6128..35150dd87f76 100644
--- a/include/linux/nvme-fc-driver.h
+++ b/include/linux/nvme-fc-driver.h
@@ -511,6 +511,8 @@ struct nvme_fc_port_template {
 	u32	remote_priv_sz;
 	u32	lsrqst_priv_sz;
 	u32	fcprqst_priv_sz;
+
+	void	*xs_kabi_padding;
 };
 
 
diff --git a/include/linux/pagemap.h b/include/linux/pagemap.h
index 15793a4af9d4..333766cb86e2 100644
--- a/include/linux/pagemap.h
+++ b/include/linux/pagemap.h
@@ -1277,6 +1277,7 @@ struct readahead_control {
 	struct file *file;
 	struct address_space *mapping;
 	struct file_ra_state *ra;
+	void *xs_kabi_padding;
 /* private: use the readahead_* accessors instead */
 	pgoff_t _index;
 	unsigned int _nr_pages;
diff --git a/include/linux/pci.h b/include/linux/pci.h
index a1e4de68edee..7cac4743a0cb 100644
--- a/include/linux/pci.h
+++ b/include/linux/pci.h
@@ -529,6 +529,8 @@ struct pci_dev {
 
 	/* These methods index pci_reset_fn_methods[] */
 	u8 reset_methods[PCI_NUM_RESET_METHODS]; /* In priority order */
+
+	void	*xs_kabi_padding;
 };
 
 static inline struct pci_dev *pci_physfn(struct pci_dev *dev)
@@ -678,6 +680,8 @@ struct pci_bus {
 	struct bin_attribute	*legacy_mem;	/* Legacy mem */
 	unsigned int		is_added:1;
 	unsigned int		unsafe_warn:1;	/* warned about RW1C config write */
+
+	void	*xs_kabi_padding;
 };
 
 #define to_pci_bus(n)	container_of(n, struct pci_bus, dev)
@@ -933,6 +937,7 @@ struct pci_driver {
 	struct device_driver	driver;
 	struct pci_dynids	dynids;
 	bool driver_managed_dma;
+	void	*xs_kabi_padding;
 };
 
 static inline struct pci_driver *to_pci_driver(struct device_driver *drv)
diff --git a/include/linux/sched.h b/include/linux/sched.h
index 393c300347de..584e748b4fd0 100644
--- a/include/linux/sched.h
+++ b/include/linux/sched.h
@@ -504,6 +504,7 @@ struct sched_avg {
 	unsigned long			runnable_avg;
 	unsigned long			util_avg;
 	struct util_est			util_est;
+	void	*xs_kabi_padding;
 } ____cacheline_aligned;
 
 struct sched_statistics {
@@ -587,6 +588,8 @@ struct sched_entity {
 	 */
 	struct sched_avg		avg;
 #endif
+
+	void    *xs_kabi_padding;
 };
 
 struct sched_rt_entity {
@@ -605,6 +608,8 @@ struct sched_rt_entity {
 	/* rq "owned" by this entity/group: */
 	struct rt_rq			*my_q;
 #endif
+
+	void	*xs_kabi_padding;
 } __randomize_layout;
 
 struct sched_dl_entity {
@@ -678,6 +683,8 @@ struct sched_dl_entity {
 	 */
 	struct sched_dl_entity *pi_se;
 #endif
+
+	void	*xs_kabi_padding;
 };
 
 #ifdef CONFIG_UCLAMP_TASK
@@ -712,6 +719,7 @@ struct uclamp_se {
 	unsigned int bucket_id		: bits_per(UCLAMP_BUCKETS);
 	unsigned int active		: 1;
 	unsigned int user_defined	: 1;
+	void	*xs_kabi_padding;
 };
 #endif /* CONFIG_UCLAMP_TASK */
 
@@ -1542,6 +1550,8 @@ struct task_struct {
 	struct user_event_mm		*user_event_mm;
 #endif
 
+	void	*xs_kabi_padding;
+
 	/*
 	 * New fields for task_struct should be added above here, so that
 	 * they are included in the randomized portion of task_struct.
diff --git a/include/linux/sched/signal.h b/include/linux/sched/signal.h
index 0014d3adaf84..f9f1b45431fb 100644
--- a/include/linux/sched/signal.h
+++ b/include/linux/sched/signal.h
@@ -245,6 +245,7 @@ struct signal_struct {
 						 * and may have inconsistent
 						 * permissions.
 						 */
+	void *xs_kabi_padding;
 } __randomize_layout;
 
 /*
diff --git a/include/linux/sched/topology.h b/include/linux/sched/topology.h
index 67b573d5bf28..8d80e2e5592d 100644
--- a/include/linux/sched/topology.h
+++ b/include/linux/sched/topology.h
@@ -151,6 +151,8 @@ struct sched_domain {
 	};
 	struct sched_domain_shared *shared;
 
+	void *xs_kabi_padding;
+
 	unsigned int span_weight;
 	/*
 	 * Span of all CPUs in this domain.
diff --git a/include/linux/sched/user.h b/include/linux/sched/user.h
index 4cc52698e214..2791a8691540 100644
--- a/include/linux/sched/user.h
+++ b/include/linux/sched/user.h
@@ -34,6 +34,8 @@ struct user_struct {
 
 	/* Miscellaneous per-user rate limit */
 	struct ratelimit_state ratelimit;
+
+	void *xs_kabi_padding;
 };
 
 extern int uids_sysfs_init(void);
diff --git a/include/linux/shrinker.h b/include/linux/shrinker.h
index 224293b2dd06..141246e1c2c8 100644
--- a/include/linux/shrinker.h
+++ b/include/linux/shrinker.h
@@ -83,6 +83,7 @@ struct shrinker {
 #endif
 	/* objs pending delete, per node */
 	atomic_long_t *nr_deferred;
+	void *xs_kabi_padding;
 };
 #define DEFAULT_SEEKS 2 /* A good number if you don't know better. */
 
diff --git a/include/linux/skbuff.h b/include/linux/skbuff.h
index f7d392d849be..0f8313b6f3e8 100644
--- a/include/linux/skbuff.h
+++ b/include/linux/skbuff.h
@@ -1044,6 +1044,8 @@ struct sk_buff {
 	u64			kcov_handle;
 #endif
 
+	void    *xs_kabi_padding;
+
 	); /* end headers group */
 
 	/* These elements must be at the end, see alloc_skb() for details.  */
diff --git a/include/linux/swap.h b/include/linux/swap.h
index cb25db2a93dd..275705e392af 100644
--- a/include/linux/swap.h
+++ b/include/linux/swap.h
@@ -321,6 +321,7 @@ struct swap_info_struct {
 					 */
 	struct work_struct discard_work; /* discard worker */
 	struct swap_cluster_list discard_clusters; /* discard clusters list */
+	void *xs_kabi_padding;
 	struct plist_node avail_lists[]; /*
 					   * entries in swap_avail_heads, one
 					   * entry per node.
diff --git a/include/linux/timer.h b/include/linux/timer.h
index 9162f275819a..7ab38185568d 100644
--- a/include/linux/timer.h
+++ b/include/linux/timer.h
@@ -21,6 +21,7 @@ struct timer_list {
 #ifdef CONFIG_LOCKDEP
 	struct lockdep_map	lockdep_map;
 #endif
+	void			*xs_kabi_padding;
 };
 
 #ifdef CONFIG_LOCKDEP
diff --git a/include/linux/trace_events.h b/include/linux/trace_events.h
index fe95d13c5e4d..968e57e877a3 100644
--- a/include/linux/trace_events.h
+++ b/include/linux/trace_events.h
@@ -113,6 +113,8 @@ struct trace_iterator {
 	/* it's true when current open file is snapshot */
 	bool			snapshot;
 
+	void	*xs_kabi_padding;
+
 	/* The below is zeroed out in pipe_read */
 	struct trace_seq	seq;
 	struct trace_entry	*ent;
@@ -681,6 +683,7 @@ struct trace_event_file {
 	atomic_t		ref;	/* ref count for opened files */
 	atomic_t		sm_ref;	/* soft-mode reference counter */
 	atomic_t		tm_ref;	/* trigger-mode reference counter */
+	void	*xs_kabi_padding;
 };
 
 #ifdef CONFIG_HIST_TRIGGERS
diff --git a/include/linux/usb/hcd.h b/include/linux/usb/hcd.h
index d0e19ac3ba6c..9bd5f28ae9ab 100644
--- a/include/linux/usb/hcd.h
+++ b/include/linux/usb/hcd.h
@@ -213,6 +213,8 @@ struct usb_hcd {
 	 * (ohci 32, uhci 1024, ehci 256/512/1024).
 	 */
 
+	void *xs_kabi_padding;
+
 	/* The HC driver's private data is stored at the end of
 	 * this structure.
 	 */
diff --git a/include/net/dcbnl.h b/include/net/dcbnl.h
index 42207fc44660..1d5dc2bb0804 100644
--- a/include/net/dcbnl.h
+++ b/include/net/dcbnl.h
@@ -131,6 +131,8 @@ struct dcbnl_rtnl_ops {
 	/* rewrite */
 	int (*dcbnl_setrewr)(struct net_device *dev, struct dcb_app *app);
 	int (*dcbnl_delrewr)(struct net_device *dev, struct dcb_app *app);
+
+	void *xs_kabi_padding;
 };
 
 #endif /* __NET_DCBNL_H__ */
diff --git a/include/net/devlink.h b/include/net/devlink.h
index 29fd1b4ee654..8935110d6583 100644
--- a/include/net/devlink.h
+++ b/include/net/devlink.h
@@ -150,6 +150,8 @@ struct devlink_port {
 
 	struct devlink_rate *devlink_rate;
 	struct devlink_linecard *linecard;
+
+	void	*xs_kabi_padding;
 };
 
 struct devlink_port_new_attrs {
diff --git a/include/net/dst.h b/include/net/dst.h
index 16b7b99b5f30..877676841e11 100644
--- a/include/net/dst.h
+++ b/include/net/dst.h
@@ -92,6 +92,8 @@ struct dst_entry {
 #ifdef CONFIG_64BIT
 	struct lwtunnel_state   *lwtstate;
 #endif
+
+	void	*xs_kabi_padding;
 };
 
 struct dst_metrics {
diff --git a/include/net/dst_ops.h b/include/net/dst_ops.h
index 3a9001a042a5..ecc2334a5665 100644
--- a/include/net/dst_ops.h
+++ b/include/net/dst_ops.h
@@ -41,6 +41,8 @@ struct dst_ops {
 	struct kmem_cache	*kmem_cachep;
 
 	struct percpu_counter	pcpuc_entries ____cacheline_aligned_in_smp;
+
+	void			*xs_kabi_padding;
 };
 
 static inline int dst_entries_get_fast(struct dst_ops *dst)
diff --git a/include/net/fib_rules.h b/include/net/fib_rules.h
index 82da359bca03..9f0ec1a4fc07 100644
--- a/include/net/fib_rules.h
+++ b/include/net/fib_rules.h
@@ -44,6 +44,8 @@ struct fib_rule {
 	struct fib_rule_port_range	sport_range;
 	struct fib_rule_port_range	dport_range;
 	struct rcu_head		rcu;
+
+	void			*xs_kabi_padding;
 };
 
 struct fib_lookup_arg {
diff --git a/include/net/flow.h b/include/net/flow.h
index 335bbc52171c..0b4f803aa52b 100644
--- a/include/net/flow.h
+++ b/include/net/flow.h
@@ -42,6 +42,8 @@ struct flowi_common {
 	kuid_t  flowic_uid;
 	__u32		flowic_multipath_hash;
 	struct flowi_tunnel flowic_tun_key;
+
+	void	*xs_kabi_padding;
 };
 
 union flowi_uli {
@@ -88,6 +90,8 @@ struct flowi4 {
 #define fl4_icmp_code		uli.icmpt.code
 #define fl4_mh_type		uli.mht.type
 #define fl4_gre_key		uli.gre_key
+
+	void	*xs_kabi_padding;
 } __attribute__((__aligned__(BITS_PER_LONG/8)));
 
 static inline void flowi4_init_output(struct flowi4 *fl4, int oif,
@@ -149,6 +153,8 @@ struct flowi6 {
 #define fl6_mh_type		uli.mht.type
 #define fl6_gre_key		uli.gre_key
 	__u32			mp_hash;
+
+	void	*xs_kabi_padding;
 } __attribute__((__aligned__(BITS_PER_LONG/8)));
 
 struct flowi {
diff --git a/include/net/genetlink.h b/include/net/genetlink.h
index e8c34aa4a640..6fd07493d25d 100644
--- a/include/net/genetlink.h
+++ b/include/net/genetlink.h
@@ -89,6 +89,8 @@ struct genl_family {
 	int			id;
 	/* starting number of multicast group IDs in this family */
 	unsigned int		mcgrp_offset;
+
+	void			*xs_kabi_padding;
 };
 
 /**
@@ -167,6 +169,8 @@ struct genl_small_ops {
 	u8	internal_flags;
 	u8	flags;
 	u8	validate;
+
+	void *xs_kabi_padding;
 };
 
 /**
@@ -195,6 +199,8 @@ struct genl_ops {
 	u8			internal_flags;
 	u8			flags;
 	u8			validate;
+
+	void			*xs_kabi_padding;
 };
 
 /**
@@ -247,6 +253,8 @@ struct genl_split_ops {
 	u8			internal_flags;
 	u8			flags;
 	u8			validate;
+
+	void			*xs_kabi_padding;
 };
 
 /**
diff --git a/include/net/ip6_fib.h b/include/net/ip6_fib.h
index 1121d614942c..125c0d45ca4c 100644
--- a/include/net/ip6_fib.h
+++ b/include/net/ip6_fib.h
@@ -203,6 +203,9 @@ struct fib6_info {
 
 	struct rcu_head			rcu;
 	struct nexthop			*nh;
+
+	void			*xs_kabi_padding;
+
 	struct fib6_nh			fib6_nh[];
 };
 
diff --git a/include/net/ip_fib.h b/include/net/ip_fib.h
index ca1700c2a573..414258cb1e08 100644
--- a/include/net/ip_fib.h
+++ b/include/net/ip_fib.h
@@ -157,6 +157,9 @@ struct fib_info {
 	bool			pfsrc_removed;
 	struct nexthop		*nh;
 	struct rcu_head		rcu;
+
+	void		*xs_kabi_padding;
+
 	struct fib_nh		fib_nh[];
 };
 
@@ -256,6 +259,9 @@ struct fib_table {
 	int			tb_num_default;
 	struct rcu_head		rcu;
 	unsigned long 		*tb_data;
+
+	void			*xs_kabi_padding;
+
 	unsigned long		__data[];
 };
 
diff --git a/include/net/l3mdev.h b/include/net/l3mdev.h
index bdfa9d414360..cd8e33ea011e 100644
--- a/include/net/l3mdev.h
+++ b/include/net/l3mdev.h
@@ -43,6 +43,8 @@ struct l3mdev_ops {
 	/* IPv6 ops */
 	struct dst_entry * (*l3mdev_link_scope_lookup)(const struct net_device *dev,
 						 struct flowi6 *fl6);
+
+	void		*xs_kabi_padding;
 };
 
 #ifdef CONFIG_NET_L3_MASTER_DEV
diff --git a/include/net/lwtunnel.h b/include/net/lwtunnel.h
index 53bd2d02a4f0..e97619e89c8a 100644
--- a/include/net/lwtunnel.h
+++ b/include/net/lwtunnel.h
@@ -33,6 +33,9 @@ struct lwtunnel_state {
 	int		(*orig_output)(struct net *net, struct sock *sk, struct sk_buff *skb);
 	int		(*orig_input)(struct sk_buff *);
 	struct		rcu_head rcu;
+
+	void		*xs_kabi_padding;
+
 	__u8            data[];
 };
 
diff --git a/include/net/macsec.h b/include/net/macsec.h
index 75340c3e0c8b..d99bdacbbd29 100644
--- a/include/net/macsec.h
+++ b/include/net/macsec.h
@@ -304,6 +304,8 @@ struct macsec_ops {
 	int (*mdo_get_rx_sc_stats)(struct macsec_context *ctx);
 	int (*mdo_get_rx_sa_stats)(struct macsec_context *ctx);
 	bool rx_uses_md_dst;
+
+	void *xs_kabi_padding;
 };
 
 void macsec_pn_wrapped(struct macsec_secy *secy, struct macsec_tx_sa *tx_sa);
diff --git a/include/net/neighbour.h b/include/net/neighbour.h
index 0d28172193fa..e86f6a4ca67c 100644
--- a/include/net/neighbour.h
+++ b/include/net/neighbour.h
@@ -162,6 +162,9 @@ struct neighbour {
 	struct rcu_head		rcu;
 	struct net_device	*dev;
 	netdevice_tracker	dev_tracker;
+
+	void	*xs_kabi_padding;
+
 	u8			primary_key[];
 } __randomize_layout;
 
diff --git a/include/net/netdev_rx_queue.h b/include/net/netdev_rx_queue.h
index cdcafb30d437..9e8f8d105c1f 100644
--- a/include/net/netdev_rx_queue.h
+++ b/include/net/netdev_rx_queue.h
@@ -21,6 +21,8 @@ struct netdev_rx_queue {
 #ifdef CONFIG_XDP_SOCKETS
 	struct xsk_buff_pool            *pool;
 #endif
+
+	void			*xs_kabi_padding;
 } ____cacheline_aligned_in_smp;
 
 /*
diff --git a/include/net/netlink.h b/include/net/netlink.h
index 8a7cd1170e1f..49adedbb575f 100644
--- a/include/net/netlink.h
+++ b/include/net/netlink.h
@@ -359,6 +359,8 @@ struct nla_policy {
 		int (*validate)(const struct nlattr *attr,
 				struct netlink_ext_ack *extack);
 	};
+
+	void	*xs_kabi_padding;
 };
 
 #define NLA_POLICY_ETH_ADDR		NLA_POLICY_EXACT_LEN(ETH_ALEN)
diff --git a/include/net/nexthop.h b/include/net/nexthop.h
index 2b12725de9c0..6b16bac06a66 100644
--- a/include/net/nexthop.h
+++ b/include/net/nexthop.h
@@ -125,6 +125,8 @@ struct nh_group {
 	bool			fdb_nh;
 	bool			has_v4;
 
+	void			*xs_kabi_padding;
+
 	struct nh_res_table __rcu *res_table;
 	struct nh_grp_entry	nh_entries[];
 };
diff --git a/include/net/rtnetlink.h b/include/net/rtnetlink.h
index c1fa6fee0acf..13406dea881d 100644
--- a/include/net/rtnetlink.h
+++ b/include/net/rtnetlink.h
@@ -152,6 +152,8 @@ struct rtnl_link_ops {
 	int			(*fill_linkxstats)(struct sk_buff *skb,
 						   const struct net_device *dev,
 						   int *prividx, int attr);
+
+	void			*xs_kabi_padding;
 };
 
 int __rtnl_link_register(struct rtnl_link_ops *ops);
diff --git a/include/net/sch_generic.h b/include/net/sch_generic.h
index 3287988a6a98..74aed4977aae 100644
--- a/include/net/sch_generic.h
+++ b/include/net/sch_generic.h
@@ -127,6 +127,9 @@ struct Qdisc {
 	struct rcu_head		rcu;
 	netdevice_tracker	dev_tracker;
 	struct lock_class_key	root_lock_key;
+
+	void *xs_kabi_padding;
+
 	/* private data */
 	long privdata[] ____cacheline_aligned;
 };
diff --git a/include/net/sock.h b/include/net/sock.h
index e15bea43b2ec..c203f18535af 100644
--- a/include/net/sock.h
+++ b/include/net/sock.h
@@ -545,6 +545,8 @@ struct sock {
 	struct rcu_head		sk_rcu;
 	netns_tracker		ns_tracker;
 	struct hlist_node	sk_bind2_node;
+
+	void			*xs_kabi_padding;
 };
 
 enum sk_pacing {
@@ -1369,6 +1371,8 @@ struct proto {
 
 	struct list_head	node;
 	int			(*diag_destroy)(struct sock *sk, int err);
+
+	void			*xs_kabi_padding;
 } __randomize_layout;
 
 int proto_register(struct proto *prot, int alloc_slab);
diff --git a/include/net/tls.h b/include/net/tls.h
index 6c642ea18050..396c68ee1453 100644
--- a/include/net/tls.h
+++ b/include/net/tls.h
@@ -191,6 +191,8 @@ enum tls_context_flags {
 struct cipher_context {
 	char *iv;
 	char *rec_seq;
+
+	void *xs_kabi_padding;
 };
 
 union tls_crypto_context {
@@ -202,6 +204,8 @@ union tls_crypto_context {
 		struct tls12_crypto_info_sm4_gcm sm4_gcm;
 		struct tls12_crypto_info_sm4_ccm sm4_ccm;
 	};
+
+	void *xs_kabi_padding;
 };
 
 struct tls_prot_info {
@@ -279,6 +283,8 @@ struct tlsdev_ops {
 	int (*tls_dev_resync)(struct net_device *netdev,
 			      struct sock *sk, u32 seq, u8 *rcd_sn,
 			      enum tls_offload_ctx_dir direction);
+
+	void *xs_kabi_padding;
 };
 
 enum tls_offload_sync_type {
diff --git a/include/net/udp_tunnel.h b/include/net/udp_tunnel.h
index 29251c3519cf..b21d744dc181 100644
--- a/include/net/udp_tunnel.h
+++ b/include/net/udp_tunnel.h
@@ -120,6 +120,8 @@ struct udp_tunnel_info {
 	sa_family_t sa_family;
 	__be16 port;
 	u8 hw_priv;
+
+	void *xs_kabi_padding;
 };
 
 /* Notify network devices of offloadable types */
@@ -212,6 +214,8 @@ struct udp_tunnel_nic_shared {
 	struct udp_tunnel_nic *udp_tunnel_nic_info;
 
 	struct list_head devices;
+
+	void *xs_kabi_padding;
 };
 
 struct udp_tunnel_nic_shared_node {
@@ -267,7 +271,11 @@ struct udp_tunnel_nic_info {
 	struct udp_tunnel_nic_table_info {
 		unsigned int n_entries;
 		unsigned int tunnel_types;
+
+		void *xs_kabi_padding;
 	} tables[UDP_TUNNEL_NIC_MAX_TABLES];
+
+	void *xs_kabi_padding;
 };
 
 /* UDP tunnel module dependencies
diff --git a/include/net/xfrm.h b/include/net/xfrm.h
index fd550c0b5634..6997e41378ee 100644
--- a/include/net/xfrm.h
+++ b/include/net/xfrm.h
@@ -124,6 +124,8 @@ struct xfrm_state_walk {
 	u8			proto;
 	u32			seq;
 	struct xfrm_address_filter *filter;
+
+	void		*xs_kabi_padding;
 };
 
 enum {
@@ -150,6 +152,8 @@ struct xfrm_dev_offload {
 	u8			dir : 2;
 	u8			type : 2;
 	u8			flags : 2;
+
+	void		*xs_kabi_padding;
 };
 
 struct xfrm_mode {
diff --git a/include/net/xsk_buff_pool.h b/include/net/xsk_buff_pool.h
index b0bdff26fc88..f7c80b9220c8 100644
--- a/include/net/xsk_buff_pool.h
+++ b/include/net/xsk_buff_pool.h
@@ -87,6 +87,7 @@ struct xsk_buff_pool {
 	 * sockets share a single cq when the same netdev and queue id is shared.
 	 */
 	spinlock_t cq_lock;
+	void *xs_kabi_padding;
 	struct xdp_buff_xsk *free_heads[];
 };
 
diff --git a/include/scsi/scsi_cmnd.h b/include/scsi/scsi_cmnd.h
index 6ae00983a612..2498a85ee71e 100644
--- a/include/scsi/scsi_cmnd.h
+++ b/include/scsi/scsi_cmnd.h
@@ -141,6 +141,8 @@ struct scsi_cmnd {
 					 * to be at an address < 16Mb). */
 
 	int result;		/* Status code from lower level driver */
+
+	void	*xs_kabi_padding;
 };
 
 /* Variant of blk_mq_rq_from_pdu() that verifies the type of its argument. */
diff --git a/include/scsi/scsi_device.h b/include/scsi/scsi_device.h
index c38f4fe5e64c..607fdefd8f81 100644
--- a/include/scsi/scsi_device.h
+++ b/include/scsi/scsi_device.h
@@ -280,6 +280,9 @@ struct scsi_device {
 	struct mutex		state_mutex;
 	enum scsi_device_state sdev_state;
 	struct task_struct	*quiesced_by;
+
+	void				*xs_kabi_padding;
+
 	unsigned long		sdev_data[];
 } __attribute__((aligned(sizeof(unsigned long))));
 
@@ -366,6 +369,9 @@ struct scsi_target {
 	char			scsi_level;
 	enum scsi_target_state	state;
 	void 			*hostdata; /* available to low-level driver */
+
+	void			*xs_kabi_padding;
+
 	unsigned long		starget_data[]; /* for the transport */
 	/* starget_data must be the last element!!!! */
 } __attribute__((aligned(sizeof(unsigned long))));
diff --git a/include/scsi/scsi_host.h b/include/scsi/scsi_host.h
index f9d5ce6170a7..8dc06528dfb5 100644
--- a/include/scsi/scsi_host.h
+++ b/include/scsi/scsi_host.h
@@ -497,6 +497,8 @@ struct scsi_host_template {
 
 	/* Delay for runtime autosuspend */
 	int rpm_autosuspend_delay;
+
+	void	*xs_kabi_padding;
 };
 
 /*
@@ -710,6 +712,8 @@ struct Scsi_Host {
 	 */
 	struct device *dma_dev;
 
+	void	*xs_kabi_padding;
+
 	/*
 	 * We should ensure that this is aligned, both for better performance
 	 * and also because some compilers (m68k) don't automatically force
diff --git a/include/scsi/scsi_transport.h b/include/scsi/scsi_transport.h
index a0458bda3148..7da670a2b156 100644
--- a/include/scsi/scsi_transport.h
+++ b/include/scsi/scsi_transport.h
@@ -43,6 +43,8 @@ struct scsi_transport_template {
 	 * Allows a transport to override the default error handler.
 	 */
 	void (* eh_strategy_handler)(struct Scsi_Host *);
+
+	void	*xs_kabi_padding;
 };
 
 #define transport_class_to_shost(tc) \
diff --git a/include/scsi/scsi_transport_fc.h b/include/scsi/scsi_transport_fc.h
index 483513c57597..8c1aa9cd1dcd 100644
--- a/include/scsi/scsi_transport_fc.h
+++ b/include/scsi/scsi_transport_fc.h
@@ -765,6 +765,8 @@ struct fc_function_template {
 	unsigned long	show_host_system_hostname:1;
 
 	unsigned long	disable_target_scan:1;
+
+	void	*xs_kabi_padding;
 };
 
 /**
diff --git a/include/scsi/scsi_transport_iscsi.h b/include/scsi/scsi_transport_iscsi.h
index fb3399e4cd29..d209ec4c3160 100644
--- a/include/scsi/scsi_transport_iscsi.h
+++ b/include/scsi/scsi_transport_iscsi.h
@@ -156,6 +156,8 @@ struct iscsi_transport {
 	int (*logout_flashnode_sid) (struct iscsi_cls_session *cls_sess);
 	int (*get_host_stats) (struct Scsi_Host *shost, char *buf, int len);
 	u8 (*check_protection)(struct iscsi_task *task, sector_t *sector);
+
+	void	*xs_kabi_padding;
 };
 
 /*
diff --git a/include/scsi/scsi_transport_sas.h b/include/scsi/scsi_transport_sas.h
index e3b6ce3cbf88..7000a8184531 100644
--- a/include/scsi/scsi_transport_sas.h
+++ b/include/scsi/scsi_transport_sas.h
@@ -182,6 +182,8 @@ struct sas_function_template {
 	int (*set_phy_speed)(struct sas_phy *, struct sas_phy_linkrates *);
 	void (*smp_handler)(struct bsg_job *, struct Scsi_Host *,
 			struct sas_rphy *);
+
+	void	*xs_kabi_padding;
 };
 
 
diff --git a/kernel/sched/sched.h b/kernel/sched/sched.h
index 60dc51f43dd9..980fa851efb6 100644
--- a/kernel/sched/sched.h
+++ b/kernel/sched/sched.h
@@ -284,6 +284,7 @@ struct rt_bandwidth {
 	u64			rt_runtime;
 	struct hrtimer		rt_period_timer;
 	unsigned int		rt_period_active;
+	void		*xs_kabi_padding;
 };
 
 static inline int dl_bandwidth_enabled(void)
@@ -313,6 +314,7 @@ struct dl_bw {
 	raw_spinlock_t		lock;
 	u64			bw;
 	u64			total_bw;
+	void		*xs_kabi_padding;
 };
 
 extern void init_dl_bw(struct dl_bw *dl_b);
@@ -357,6 +359,7 @@ struct cfs_bandwidth {
 	u64			throttled_time;
 	u64			burst_time;
 #endif
+	void			*xs_kabi_padding;
 };
 
 /* Task group related information */
@@ -412,6 +415,7 @@ struct task_group {
 	struct uclamp_se	uclamp[UCLAMP_CNT];
 #endif
 
+	void		*xs_kabi_padding;
 };
 
 #ifdef CONFIG_FAIR_GROUP_SCHED
@@ -649,6 +653,8 @@ struct cfs_rq {
 #endif
 #endif /* CONFIG_CFS_BANDWIDTH */
 #endif /* CONFIG_FAIR_GROUP_SCHED */
+
+	void		*xs_kabi_padding;
 };
 
 static inline int rt_bandwidth_enabled(void)
@@ -695,6 +701,8 @@ struct rt_rq {
 	struct rq		*rq;
 	struct task_group	*tg;
 #endif
+
+	void		*xs_kabi_padding;
 };
 
 static inline bool rt_rq_is_runnable(struct rt_rq *rt_rq)
@@ -763,6 +771,8 @@ struct dl_rq {
 	 * by the GRUB algorithm.
 	 */
 	u64			bw_ratio;
+
+	void			*xs_kabi_padding;
 };
 
 #ifdef CONFIG_FAIR_GROUP_SCHED
@@ -889,6 +899,8 @@ struct root_domain {
 	 * CPUs of the rd. Protected by RCU.
 	 */
 	struct perf_domain __rcu *pd;
+
+	void		*xs_kabi_padding;
 };
 
 extern void init_defrootdomain(void);
@@ -1166,6 +1178,8 @@ struct rq {
 	call_single_data_t	cfsb_csd;
 	struct list_head	cfsb_csd_list;
 #endif
+
+	void		*xs_kabi_padding;
 };
 
 #ifdef CONFIG_FAIR_GROUP_SCHED
diff --git a/net/devlink/devl_internal.h b/net/devlink/devl_internal.h
index f6b5fea2e13c..8ab8ec3ea95b 100644
--- a/net/devlink/devl_internal.h
+++ b/net/devlink/devl_internal.h
@@ -55,6 +55,7 @@ struct devlink {
 	u8 reload_failed:1;
 	refcount_t refcount;
 	struct rcu_work rwork;
+	void	*xs_kabi_padding;
 	char priv[] __aligned(NETDEV_ALIGN);
 };
 
-- 
2.34.1

